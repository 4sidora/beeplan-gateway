#pragma once

#define WIFI_SSID "{{WIFI_SSID}}"
#define WIFI_PASSWORD "{{WIFI_PASSWORD}}"

/** Базовый URL API без завершающего слэша. */
#define API_BASE_URL "{{API_BASE_URL}}"

/** Токен концентратора (ingest_token) из BeePlan API. */
#define INGEST_TOKEN "{{INGEST_TOKEN}}"

/** Версия прошивки (semver или git tag). */
#define FIRMWARE_VERSION "{{FIRMWARE_VERSION}}"
